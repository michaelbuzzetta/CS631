I started this assignment by just creating the basic function call and all of the imports

I then implimented the -h flag first as that seemed the most simple, as it was a simple print statement

I then tested that step with several inputs that I knew would error like ./everett -x

After I was satisfied with the tests, I commited that to github and began working on the -w flag

For this one, the code was aslo pretty straight forward. I tested this flag with both words and random characters

Once I was satisfied woth the implimentation of -w, I commited my word to github

At this point, I noticed that I forgot to return Exit_SUCCESS; so I quickly added that line and commited it as well

then I tackled -n. I implimented and tested this flag. I tested it with normal integers, floats, ans strings

I was debating adding input validation to ensure that the user enters a whole number for the -n flag, and a non-empty string for the -w flag, but that seemed overly complex for the assignment

Overall, this was a very effective introduction to C